// (Optional) mock constants, kept for compatibility with the requested filename.
// The app reads directly from `src/mock/data/dashboardData.js`.

export const mockData = {
  widgetDockAlignment: 'top',
}

